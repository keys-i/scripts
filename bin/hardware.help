---
{
  "summary": "Diagnose hardware and apply conservative native repairs without hiding platform tools.",
  "platform": "any",
  "applyFlag": "--apply",
  "yesFlag": "--yes",
  "parameters": [
    {
      "name": "action",
      "label": "Action",
      "required": true,
      "default": "diagnose",
      "choices": [
        {"value": "diagnose", "label": "Diagnose — collect native hardware evidence"},
        {"value": "fix", "label": "Fix — preview one conservative software repair"}
      ]
    },
    {
      "name": "area",
      "label": "Hardware area",
      "flag": "--area",
      "required": true,
      "default": "all",
      "choices": [
        {"value": "all", "label": "All available hardware"},
        {"value": "cpu", "label": "Processor"},
        {"value": "memory", "label": "Memory"},
        {"value": "display", "label": "Display adapter and login manager"},
        {"value": "storage", "label": "Storage (delegates to disk)"},
        {"value": "network", "label": "Network adapters and DNS"},
        {"value": "battery", "label": "Battery and power"}
      ]
    },
    {
      "name": "display-action",
      "label": "Linux display repair",
      "flag": "--display-action",
      "required": true,
      "default": "restart",
      "choices": [
        {"value": "restart", "label": "Reset and restart the active display manager"},
        {"value": "drivers", "label": "Install Ubuntu's recommended signed drivers"},
        {"value": "reconfigure", "label": "Reconfigure GDM3 in a direct terminal"}
      ],
      "warning": "Restarting the display manager ends graphical sessions. Reconfigure needs a direct terminal."
    },
    {
      "name": "target",
      "label": "Storage target",
      "flag": "--target",
      "placeholder": "/dev/sda1, disk2s1, or C"
    }
  ],
  "options": []
}
---

# Cross-platform hardware doctor

Collects evidence with operating-system tools instead of guessing from generic
error messages. Linux uses tools such as `lscpu`, `journalctl`, `lspci`,
`ubuntu-drivers`, `ip`, and `upower`; macOS uses `system_profiler` and `pmset`;
Windows uses CIM, PnP, and networking PowerShell cmdlets.

## Actions

### `diagnose`

Choose `--area all`, `cpu`, `memory`, `display`, `storage`, `network`, or
`battery`. Missing optional commands are skipped. Storage health is handled by
the sibling `disk` command so filesystem and SMART behavior stays in one place.

### `fix`

Fixes are deliberately narrow:

- Linux display: restart the configured display manager, run
  `ubuntu-drivers install`, or reconfigure GDM3.
- Network: flush the native DNS cache.
- Storage: delegate verification and repair to `disk fix`.

There is no generic safe software repair for failed processors, memory,
batteries, or other physical parts. Diagnosis provides evidence for vendor
tests or replacement.

The historical `gdm3-error-fix` behavior was replaced with this guarded Linux
display workflow. It does not install LightDM, change the selected display
manager, or force a reboot.

## Direct CLI

```sh
hardware diagnose --area all
hardware diagnose --area display
hardware fix --area network
hardware fix --area display --display-action drivers --apply
hardware fix --area storage --target /dev/sdb1 --apply
```

Nothing mutates without `--apply`. Direct applies require typing `APPLY`; the
shared TUI confirms with `CLEAN` and then supplies `--apply --yes`.
