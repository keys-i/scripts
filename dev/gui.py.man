---
{
  "summary": "Browse, configure, and run every documented script in a responsive terminal dashboard.",
  "platform": "any",
  "privilege": "user",
  "launchable": false,
  "parameters": [
    {
      "name": "root",
      "label": "Local scripts checkout",
      "flag": "--root",
      "placeholder": "/path/to/scripts"
    }
  ],
  "options": [
    {
      "flag": "--inline",
      "label": "Run below the prompt",
      "warning": "Inline mode is available on macOS and Linux; Windows uses full-screen mode."
    },
    {
      "flag": "--nerd-fonts",
      "label": "Use Nerd Font file and folder icons"
    },
    {
      "flag": "--self-test",
      "label": "Validate manuals, layout snapshots, keyboard, and fallbacks"
    }
  ]
}
---

# Responsive scripts dashboard

Discovers adjacent `.man` manuals under `bin`, `dev`, and `sys`, then renders
their Markdown, typed parameters, warnings, progress, files, and command output
in one keyboard- and mouse-friendly interface.

> **Apply safety:** Destructive commands always preview first and require
> `CLEAN`. Cancel and Quit are disabled during apply because interrupting
> filesystem or Git maintenance can corrupt data.

External process termination is still outside the dashboard's control.
Elevated Linux/macOS repairs fail fast unless sudo/doas policy permits
non-interactive use; a normal terminal credential cache usually does not carry
into the isolated child process. Run the displayed command directly when a
password is required. Launch the Windows dashboard from an Administrator
terminal for privileged actions. Commands that need their own interactive
terminal are previewed in the dashboard, which then shows the direct apply
command.

Without `--root`, the dashboard uses a local checkout when one can be found;
otherwise it downloads a temporary copy of the repository and removes it when
the dashboard closes. Running requires `uv`; network access is needed only when
the pinned Textual dependency is not cached or a repository copy must be
downloaded.

The dashboard colors headings, safety callouts, tables, links, and code with
the selected terminal theme. The `.man` source remains plain Markdown, so it
also works through `curl`, Glow, GitHub, and terminals without color.

## Keyboard and accessibility

The dashboard does not require a mouse. `Tab` and `Shift+Tab` move focus;
arrow keys move through tables, trees, menus, and tabs; `Enter` activates the
focused control; `Space` toggles a checkbox; and `Escape` closes a dialog or
cancels a safe running preview.

| Key | Action |
| --- | --- |
| `/` | focus the script filter |
| `F1` or `?` | show and focus the selected script's help |
| `F2` | open the three-step run wizard |
| `F3` | open files and focus the file filter |
| `F5` | sort the script catalog |
| `Ctrl+Q` | quit when no non-interruptible apply is running |

To run entirely from the keyboard, filter and select a script, press `F2`, use
`Tab` and the arrow keys through Options, Working directory, and Review, then
activate Run or Preview with `Enter`. Destructive apply still requires typing
`CLEAN` and pressing `Enter` after a successful preview.

While help is focused, use arrows, `Page Up`/`Page Down`, `Home`/`End`, or the
mouse wheel to scroll its full manual.

Set `NO_COLOR=1` for Textual's monochrome rendering. Standard file and folder
icons work without a patched font; `--nerd-fonts` is an explicit opt-in. The
catalog also labels states as `● Ready`, `◆ Guide`, or `○ Linux`/`macOS`/`Windows`,
so meaning never depends on color. Terminal screen-reader behavior depends on the terminal and
operating system; `./run.sh list`, `./run.sh man SCRIPT`, and direct commands
remain non-TUI alternatives.

## Options

| Option | Meaning |
| --- | --- |
| `--root PATH` | use one local scripts checkout |
| `--inline` | run below the prompt on macOS or Linux |
| `--nerd-fonts` | use Nerd Font file and folder icons |
| `--self-test` | validate manuals, layout snapshots, keyboard, and fallbacks |

## Direct CLI

```sh
uv run dev/gui.py
uv run dev/gui.py --root .
uv run dev/gui.py --inline --nerd-fonts
NO_COLOR=1 uv run dev/gui.py --root .
uv run dev/gui.py --root . --self-test
```

## Exit status

`0` means the dashboard or self-test closed normally. `1` means repository or
manual discovery failed. Invalid command-line syntax exits `2`.
