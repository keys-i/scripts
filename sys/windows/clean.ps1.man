---
{
  "summary": "Clean user, package, browser, developer, and Windows caches.",
  "platform": "windows",
  "privilege": "mixed",
  "applyFlag": "-Apply",
  "yesFlag": "-Yes",
  "options": [
    {
      "flag": "-AllTemp",
      "label": "Include recent user temp files",
      "warning": "Recent temporary files may still be in use."
    },
    {
      "flag": "-System",
      "label": "Run the separate system cleanup",
      "warning": "Requires an elevated 64-bit PowerShell process and cleans Windows component data."
    },
    {
      "flag": "-GlobalPackages",
      "label": "Clear the global NuGet package cache",
      "warning": "Projects will need to restore their NuGet packages."
    },
    {
      "flag": "-Verbose",
      "label": "Show skipped paths and failure details"
    }
  ]
}
---

# Windows cache cleaner

Builds a guarded cleanup plan for regenerable Windows, application, package,
browser, and development-tool caches. It requires Windows PowerShell 5.1 or
newer. Current-user temporary cleanup defaults to owned entries at least seven
days old; `-AllTemp` includes all owned entries.

> **Preview first:** In the TUI, nothing is deleted until the preview finishes
> successfully and you type `CLEAN`. Apply repeats discovery with the same
> settings, so targets may change between the two runs.

## What it can clean

- current-user temporary, Direct3D, Internet, and app `TempState` caches
- Edge, Chrome, Brave, Chromium, and Firefox caches
- Chocolatey, Scoop, and NuGet caches
- Delivery Optimization and the Windows component store in system mode

## Safety

Normal cleanup runs without elevation. System cleanup is deliberately separate
and requires an elevated 64-bit PowerShell process. The script validates local
fixed-drive paths, refuses protected paths, and does not follow reparse points.
Normal `-Apply` is refused from an elevated process. `-System` cannot be
combined with `-AllTemp` or `-GlobalPackages`.

Close browsers, packaged apps, and development tools before applying the plan.

## Options

| Option | Meaning |
| --- | --- |
| `-AllTemp` | include recent current-user temp files |
| `-System` | run the separate elevated Windows cleanup |
| `-GlobalPackages` | clear the global NuGet package folder |
| `-Verbose` | show skipped paths and failure details |
| `-Apply` | repeat discovery with deletion enabled |
| `-Yes` | accept the script's prompt after TUI confirmation |

## Direct CLI

```powershell
sys\windows\clean.ps1
sys\windows\clean.ps1 -Verbose
sys\windows\clean.ps1 -Apply -GlobalPackages
sys\windows\clean.ps1 -Apply
# Run separately from an elevated 64-bit PowerShell:
sys\windows\clean.ps1 -Apply -System
```

## Exit status

`0` means the preview or cleanup completed without warnings. `1` means input,
platform, privilege, path validation, or one or more cleanup operations failed.
